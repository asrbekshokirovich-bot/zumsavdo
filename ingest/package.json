{
  "name": "zumsavdo-ingest",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sweep": "node --env-file-if-exists=.env run-sweep.mjs",
    "probe": "node --env-file-if-exists=.env run-sweep.mjs --probe",
    "rollup": "node --env-file-if-exists=.env run-sweep.mjs --rollup-only"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.101.1"
  }
}
